
spring:
  application:
    name: payment-service

  config:
    import:
      - optional:configserver:http://localhost:8888

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
        group:
          enabled: true

# =========================
# PAYMENT CONFIG
# =========================
payment:
  settlement-currency: ${PAYMENT_SETTLEMENT_CURRENCY:USD}
  checkout:
    success-url: ${PAYMENT_SUCCESS_URL:http://localhost:5173/booking-success/{bookingId}}
    cancel-url: ${PAYMENT_CANCEL_URL:http://localhost:5173/booking-success/{bookingId}?payment_status=cancelled}

  stripe:
    secret-key: ${STRIPE_SECRET_KEY}
    webhook-secret: ${STRIPE_WEBHOOK_SECRET:}

  paypal:
    client-id: ${PAYPAL_CLIENT_ID}
    client-secret: ${PAYPAL_CLIENT_SECRET}
    base-url: ${PAYPAL_BASE_URL}
    webhook-id: ${PAYPAL_WEBHOOK_ID:}

  reconciliation:
    enabled: ${PAYMENT_RECONCILIATION_ENABLED:true}
    delay-ms: ${PAYMENT_RECONCILIATION_DELAY_MS:300000}

app:
  internal:
    secret: ${INTERNAL_SERVICE_SECRET:QW5xY0ZJY2dLNWV1TzZ2aDJlYk1aZzV1d3pHb3V3M2l4YkFTR0NhN2s0Q0E4VQ==}

# =========================
# RESILIENCE4J
# =========================
resilience4j:
  circuitbreaker:
    configs:
      default:
        sliding-window-type: COUNT_BASED
        sliding-window-size: 20
        minimum-number-of-calls: 10
        failure-rate-threshold: 50

        slow-call-rate-threshold: 80
        slow-call-duration-threshold: 3000ms

        wait-duration-in-open-state: 30s
        permitted-number-of-calls-in-half-open-state: 5

        automatic-transition-from-open-to-half-open-enabled: true
        register-health-indicator: true

        record-exceptions:
          - java.io.IOException
          - java.util.concurrent.TimeoutException
          - feign.FeignException

    instances:
      UserClient:
        base-config: default
        failure-rate-threshold: 40

# =========================
# EUREKA
# =========================
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

  client:
    register-with-eureka: true
    fetch-registry: true

# =========================
# ACTUATOR
# =========================
management:
  endpoints:
    web:
      exposure:
        include: health, info, circuitbreakers, circuitbreakerevents

  endpoint:
    health:
      show-details: always
    info:
      enabled: true

  health:
    circuitbreakers:
      enabled: true

  info:
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true

# =========================
# APP INFO
# =========================
info:
  app:
    name: ${spring.application.name}
    description: Handles payment processing via Stripe and PayPal
    version: 1.0.0
    environment: development
