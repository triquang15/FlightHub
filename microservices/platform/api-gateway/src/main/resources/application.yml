server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    config:
      enabled: false

    gateway:
      discovery:
        locator:
          enabled: true

# Eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/